import { PantryClient } from "./PantryClient";

export const metadata = {
  title: "la despensa — thespacepit",
  description: "what's running low. one tap to send the moto guy our order.",
};

export default function PantryPage() {
  return <PantryClient />;
}
